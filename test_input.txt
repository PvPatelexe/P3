# 1. basic builtin
pwd

# 2. basic external command
echo hello

# 3. which valid command
which ls

# 4. which builtin / invalid
which cd

# 5. setup test directory and move into it
rm -rf mysh_testdir
mkdir mysh_testdir
cd mysh_testdir
pwd

# 6. output redirection
echo alpha > a.txt
cat a.txt

# 7. input redirection
wc < a.txt

# 8. wildcard expansion
touch b.txt
touch c.log
touch foo1bar
touch foo22bar
echo *.txt
echo foo*bar

# 9. pipeline
echo one two three | wc

# 10. builtin in pipeline
pwd | wc

# 11. syntax / failure recovery
echo hi >
not_a_real_command
echo still_running

# 12. exit
exit
echo SHOULD_NOT_RUN